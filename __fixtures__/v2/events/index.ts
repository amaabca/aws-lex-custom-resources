import bot from './bot';
import bot_alias from './bot_alias';
import bot_locale from './bot_locale';
import bot_version from './bot_version';
import intent from './intent';
import slot from './slot';
import slotType from './slot-type';
import intentPriority from './intent-priority';

export default {
  bot,
  bot_alias,
  bot_locale,
  bot_version,
  intent,
  intentPriority,
  slot,
  slotType
};
